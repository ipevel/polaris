.gradle/
.kotlin/
build/
**/build/
local.properties
app/gradle.properties
*.keystore
*.jks
*.p12
.signing-env
*.apk
*.aab
.cxx/
.env
.env.*
*.log
.DS_Store
AGENTS.md
.agents/
.vscode/

# 扫描工具会话状态（不入库）
.mimosa/

# 构建发布产物
dist/

# 密钥材料（上游 mkcert 测试夹具除外，见 THIRD-PARTY-NOTICES.md）
*.pem
!kernel-core/src/foss/golang/clash/test/config/*.pem

# ---------------------------------------------------------------------------
# 本地测试数据（一律不入库）
#
# 本仓库只保留 App 源码与它运行所必需的支持文件。本地跑测试/真机验证产生的
# 截图、报告、临时产物全部留在开发机上，不上传。
#
# 两个原因：
# 1. 真机截图里带着真实界面数据（账号邮箱、面板地址、套餐与流量信息），
#    那是使用者自己的私有部署信息，不该固化进公开仓库；
# 2. 这类产物是"某个时间点本机跑出来的结果"，对代码本身没有价值，
#    反复重跑还会让仓库体积不断膨胀（单次全量截图就 20MB+）。
#
# 例外：app/src/main/res/ 下的图标是 App 的发布资源，属于"支持包"，照常入库。
# ---------------------------------------------------------------------------
design/emulator/
design/screenshots/
design/screenshots-sweep/
*测试报告*.md
Polaris-*.md
