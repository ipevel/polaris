<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 仅标准 HTTPS 验证（信任系统证书），不做域名 pin：
         API 地址由 OSS 远程配置随时切换（任意域名），pin 会阻碍切换 -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
