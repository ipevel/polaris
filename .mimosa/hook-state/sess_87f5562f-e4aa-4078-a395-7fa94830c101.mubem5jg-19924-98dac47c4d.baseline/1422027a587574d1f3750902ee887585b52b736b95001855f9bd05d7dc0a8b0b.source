package cc.polaris.desktop.panel.remote

import java.io.IOException

/**
 * 面板 API 异常（批1 移植件；批2 接口衔接点改动）。
 *
 * 批1 原样照抄安卓签名 `stringResId: Int?`（Android 资源 ID 占位）；
 * 批2 R3 落地后桌面无资源系统，错误码位改为**错误文案**（`String?`）：
 * - `null` = 无预置文案，UI 直接展示 message；
 * - 非 null = 远程包 [ApiErrors] / 适配器抛出的预置中文文案（同名参数保留以便最小化迁移）。
 * grep 确认批1 全部 38 文件中仅此声明处引用该参数，无既有调用受影响。
 */
class ApiException(
    override val message: String,
    val stringResId: String? = null,
) : IOException(message)
