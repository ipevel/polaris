package cc.polaris.desktop.panel.remote.config

/** Failover 候选源抽象（R8 移植，源：data/remote/config/FailoverConfig.kt，逐行照抄）。 */
interface FailoverConfig {

    val apiBaseUrl: String

    fun apiCandidates(primary: String): List<String>
}
